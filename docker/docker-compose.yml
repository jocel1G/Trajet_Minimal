name: trajet_minimal

services:
  sqlite:
    image: alpine:3.20
    working_dir: /data
    command: >-
      sh -c "apk add --no-cache sqlite && touch /data/ride_optimizer.sqlite3 &&
      tail -f /dev/null"
    volumes:
      - ../data:/data
    restart: unless-stopped
